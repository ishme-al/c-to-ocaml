let () =
  let x : int = 2 in
  let y : int = 3 in
  let z : int = 4 in
  let a : int = x + y in
  let b : int = z - y in
  let c : int = z * y in
  let d : int = z / y in
  let e : float = 13 in
  exit 0
